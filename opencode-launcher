#!/bin/bash

# OpenCode Launcher with Automation
# Runs your automation setup, then launches OpenCode

echo "🚀 OpenCode Launcher with Automation"
echo "======================================"

# Run your automation setup from the script directory
cd "$(dirname "$0")"

# Optional: Uncomment what you want to run automatically
# ./master-automation.sh
# node deployment-manager.js monitor
# source .opencode-init

echo ""
echo "✅ Automation complete - Starting OpenCode..."
echo ""

# Launch the real OpenCode CLI with any passed arguments
exec /usr/bin/opencode "$@"
